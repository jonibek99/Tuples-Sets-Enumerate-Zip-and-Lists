def swap_values(a: int, b: int) -> tuple:
    """
    Swap the values of two variables using a tuple.
    Args:
        a (int): First number
        b (int): Second number
    Returns:
        tuple: A tuple containing the swapped values (b, a)
    """
 
    return  b,a
a=1
b=21
print(swap_values(b,a))